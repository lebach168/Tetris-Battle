interface InputBuffer {
    left: boolean;   
    right: boolean;  
    up: boolean; //rotate clockwise
    rotateCounterClockwise:boolean;    
    down: boolean;   
    space: boolean;   
    hold: boolean;   

    //botActions: BotAction[]; 
  }